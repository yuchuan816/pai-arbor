info:
  name: 删除指定向量集合
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:3000/api/debug-chroma?name=user_memory_base&action=reset
  params:
    - name: name
      value: user_memory_base
      type: query
    - name: action
      value: reset
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
