info:
  name: 上传MD并处理数据
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/upload
  body:
    type: multipart-form
    data:
      - name: file
        type: text
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
