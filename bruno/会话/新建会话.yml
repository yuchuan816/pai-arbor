info:
  name: 新建会话
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
