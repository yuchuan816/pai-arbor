info:
  name: 查询所有会话
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/sessions
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
